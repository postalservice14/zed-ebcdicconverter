target/
server/target/
*.wasm

# Zed writes these into a dev extension's directory when it builds/downloads a server.
grammars/
ebcdic-lsp-*/

__pycache__/
*.pyc

# Editor/IDE state
.idea/
.DS_Store

# Local AI coding-agent guidance; the README and NOTICE are the tracked source of truth.
CLAUDE.md

# .zed/tasks.json is intentionally tracked as a usable example; ignore the rest of .zed/.
.zed/*
!.zed/tasks.json
